import styled from 'styled-components'

const SkillDiv = styled.div`
     padding: 4em 0;
     background: #424956;
    .skillContent {
        max-width: 900px;
        margin: 0 auto;
        .title {
            text-align: center;
            color: #FFF;
            margin-bottom: 2em;
        }
        .skill {
            padding: 0.5em;
        }
    }
    
`

export default SkillDiv