import styled from 'styled-components'

const SkillDiv = styled.div`
     padding: 4em 0;
    .skillContent {
        max-width: 900px;
        margin: 0 auto;
        .skill {
            text-align: center;
        }
    }
    
`

export default SkillDiv