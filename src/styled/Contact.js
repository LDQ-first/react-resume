import styled from 'styled-components'

const ContactDiv = styled.div`
     padding: 4em 0;
    .contactContent {
        max-width: 900px;
        margin: 0 auto;
        .contact {
            text-align: center;
        }
    }
    
`

export default ContactDiv