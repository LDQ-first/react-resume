import styled from 'styled-components'

const ContactDiv = styled.div`
     padding: 4em 0;
     background: #FFF;
    .contactContent {
        max-width: 900px;
        margin: 0 auto;
        .title {
            text-align: center;
            color: #6C73C4;
            margin-bottom: 2em;
        }
        .contactWrap {
            color: #6C73C4;
            .contact {

            }
        }
    }
    
`

export default ContactDiv