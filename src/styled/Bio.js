import styled from 'styled-components'

const BioDiv = styled.div`
     background: #FFF;
    .bioContent {
        max-width: 900px;
        margin: 0 auto;
        .bio {
            
        }
    }
    
`

export default BioDiv