import styled from 'styled-components'

const ProjectDiv = styled.div`
    padding: 4em 0;
    .projectContent {
        max-width: 900px;
        margin: 0 auto;
        .project {
            text-align: center;
        }
    }
    
`

export default ProjectDiv